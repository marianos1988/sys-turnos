import { createContext } from 'react'
import {ICartelConfirmarContext} from "../types/interface";

export const CartelConfirmarContext = createContext<ICartelConfirmarContext>({} as ICartelConfirmarContext);
   